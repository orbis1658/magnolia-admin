<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% if title %}{{ title }} | {% endif %}Magnolia Blog</title>
    <meta name="description" content="{{ description or 'Magnoliaブログへようこそ。技術記事や日常の記録を共有しています。' }}">
    
    <!-- Open Graph -->
    <meta property="og:title" content="{% if title %}{{ title }} | {% endif %}Magnolia Blog">
    <meta property="og:description" content="{{ description or 'Magnoliaブログへようこそ。技術記事や日常の記録を共有しています。' }}">
    <meta property="og:type" content="website">
    
    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{% if title %}{{ title }} | {% endif %}Magnolia Blog">
    <meta name="twitter:description" content="{{ description or 'Magnoliaブログへようこそ。技術記事や日常の記録を共有しています。' }}">
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Styles -->
    <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-gray-50 text-gray-900 font-sans">
    <div class="min-h-screen flex flex-col">
        {% include "header.njk" %}
        
        <main class="flex-grow">
            {% block content %}{% endblock %}
        </main>
        
        {% include "footer.njk" %}
    </div>
</body>
</html> 